# ForestCalculator v1.7.3 版本更新报告

**发布日期**: 2026-06-09  
**版本类型**: 架构审计与缺陷修复  
**GitHub Release**: https://github.com/CNJooko/ForestCalculator/releases/tag/v1.7.3

---

## 一、审计发现与修复清单

### 1. 缺陷修复（BUG）

#### 1.1 `ui.js` `calcBatch()` — 自定义出材率逻辑不一致
- **问题**: `calcBatch` 中自定义出材率逻辑与 `calcSingle` 不一致，仍依赖 DOM 读取 `getYieldRates()`
- **修复**: 改为直接传参调用 `calcYieldRates()`，与 `calcSingle` 保持一致，解耦 DOM
- **影响**: 批量计算时自定义出材率现在与单株计算行为一致

#### 1.2 `ui.js` `showCompare()` — 硬编码树种 ID 列表
- **问题**: 跨省标准对比功能中硬编码了 4 个树种组、15 个树种 ID，新增树种需手动维护
- **修复**: 改为动态分组，基于 `SPECIES` 数组自动按 `id` 前缀分组（masson-pine/cypress/chinese-fir 等）
- **影响**: 新增树种后 `showCompare` 自动适配，无需手动修改代码

#### 1.3 `tools/data_exporter.py` — `b/c` 正则误匹配 `b1/b2/c1/c2`
- **问题**: 使用 `(?<!b)b:` 负向后顾断言，但 Python `re` 模块不支持变长后顾，导致 `b1:`/`c1:` 也被匹配
- **修复**: 改用 `^\s*b:` 和 `^\s*c:` 行首匹配（`re.MULTILINE` 模式），精确匹配 `b:`/`c:` 而非 `b1:`/`c1:`
- **影响**: `complete_report.md` 中材积公式参数 now 正确提取 `b`/`c`，不再误取 `b1`/`c1`

#### 1.4 `tools/data_exporter.py` — 福建公式描述错误
- **问题**: `complete_report.md` 中福建公式标注为 "⚠️ 未集成"，实际已集成 4 个福建树种
- **修复**: 改为 "已集成（杉木福建/马尾松福建/阔叶树福建/其他针叶福建）"
- **影响**: 数据报告准确性提升

#### 1.5 `tests/test_calculator.py` — 函数名字母序错误
- **问题**: `test_all_24_econBase_species` 函数名不符合字母序规范
- **修复**: 改为 `test_all_24_species_econBase`
- **影响**: 测试函数按字母序执行，符合 unittest 约定

#### 1.6 `index.html` — footer 版本号占位符未替换
- **问题**: footer 中版本号为 `__VERSION__`，未替换为实际版本号
- **修复**: 改为 `v1.7.3`
- **影响**: 用户看到的版本号正确

---

### 2. 架构改进

#### 2.1 `ui.js` `calcBatch()` — `yTotalVal` 默认值逻辑修复
- **问题**: `yTotalVal` 默认值为 `|| 75`，当用户输入为空时 `parseFloat('')` 返回 `NaN`，`NaN || 75` 会取 `75`，导致 `isCustomYield` 误判为 `true`
- **修复**: 改为 `parseFloat(...)` 后单独判断 `isNaN`
- **影响**: 自定义出材率开关逻辑 now 正确

#### 2.2 `calculator.js` — `calcYieldRates()` 纯函数版本统一接口
- **问题**: `calcYieldRates()` 已支持 `customYTotal` 传参，但 `calcBatch` 未使用
- **修复**: `calcBatch` 现在调用 `calcYieldRates(s, isCustomYield ? yTotalVal : null)`
- **影响**: 代码一致性提升，`calcSingle` 与 `calcBatch` 共享同一纯函数

---

## 二、测试验证

- **单元测试**: 所有测试通过（Python unittest）
- **构建验证**: `build.py` 成功生成 `dist/木材蓄积量计算器.html`（102.0 KB）
- **功能验证**:
  - 单株计算 + 自定义出材率 ✅
  - 批量计算 + 自定义出材率 ✅
  - 跨省标准对比（动态分组）✅
  - 收方表生成 ✅
  - 历史记录 + 本地存储 ✅

---

## 三、文件变更清单

| 文件 | 变更类型 | 说明 |
|------|----------|------|
| `VERSION` | 修改 | 1.7.2 → 1.7.3 |
| `src/index.html` | 修改 | footer 版本号修复 |
| `src/js/ui.js` | 修改 | calcBatch 自定义出材率逻辑、showCompare 动态分组、yTotalVal 默认值修复 |
| `src/js/calculator.js` | 无变更 | calcYieldRates() 接口已支持传参 |
| `tools/data_exporter.py` | 修改 | b/c 正则修复、福建公式描述修复 |
| `tests/test_calculator.py` | 修改 | 函数名字母序修复 |
| `CHANGELOG.md` | 修改 | 新增 v1.7.3 条目 |
| `dist/木材蓄积量计算器.html` | 生成 | 构建输出 |
| `release/ForestCalculator_v1.7.3.zip` | 生成 | 发布包 |
| `release_notes_v1.7.3.md` | 生成 | Release Notes |
| `版本更新报告_v1.7.3.md` | 生成 | 本文档 |

---

## 四、Git 变更统计

```
18 files changed, 296 insertions(+), 50 deletions(-)
```

**关键提交**:
- `498ce74` — v1.7.3: 架构审计与缺陷修复

**标签**:
- `v1.7.3` — GitHub Release 已创建

---

## 五、已知问题与后续规划

### 5.1 待改进项（低优先级）
1. **`showCompare` 动态分组逻辑简化**: 当前按 `id` 前缀硬编码分组规则，可改为基于 `source` 字段自动分组
2. **`complete_report.md` 生成性能**: `data_exporter.py` 正则解析 `species-db.js` 较慢，可改为直接读取 JS 变量（需 Node.js 环境）
3. **测试覆盖率**: 当前主要覆盖计算逻辑，UI 交互测试（JSDOM）尚未集成

### 5.2 后续版本规划（v1.8.0+）
1. **PWA 离线增强**: Service Worker 缓存策略优化、离线计算历史同步
2. **数据可视化**: 批量计算结果图表（柱状图/饼图）
3. **多语言支持**: i18n 框架集成（中文/英文/拉丁语树种名）

---

## 六、使用说明

### 6.1 升级方法
1. 下载 `release/ForestCalculator_v1.7.3.zip`
2. 解压后双击 `木材蓄积量计算器.html` 即可使用（无需服务器）
3. 或直接从 `dist/` 目录使用

### 6.2 验证方法
- 检查 footer 版本号是否为 `v1.7.3`
- 测试自定义出材率：单株计算与批量计算结果应一致
- 测试跨省标准对比：点击"跨省标准对比"按钮，应看到所有树种组（不限于硬编码的 4 组）

---

**报告生成时间**: 2026-06-09  
**项目路径**: `C:\Users\10512\OneDrive\桌面\ALL\ForestCalculator`  
**项目地址**: https://github.com/CNJooko/ForestCalculator