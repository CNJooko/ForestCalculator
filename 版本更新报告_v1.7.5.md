# ForestCalculator v1.7.5 版本更新报告

## 版本概览
- **版本号**: v1.7.5
- **发布日期**: 2026-06-09
- **版本主题**: 性能监控集成
- **GitHub Release**: https://github.com/CNJooko/ForestCalculator/releases/tag/v1.7.5

## 本次更新内容

### 新增功能
1. **性能监控功能** (`ui.js`)
   - 添加 `_logPerf()` 函数，用于收集性能数据
   - 在 `renderBatch()` 和 `calcBatch()` 函数中集成性能监控
   - 使用 `performance.now()` 进行高精度计时
   - 性能日志存储在 `ForestCalc._perfLog` 数组（最多保留 50 条）
   - 控制台输出格式：`[Perf] renderBatch: 12.34 ms`

### 技术细节
- **计时精度**: 使用 `performance.now()` 提供微秒级精度
- **数据存储**: `_perfLog` 数组包含 `{ label, elapsed, time }` 对象
- **非侵入式**: 不影响现有功能和用户体验
- **日志上限**: 自动清理超过 50 条的旧日志

### 修改的文件
| 文件 | 修改类型 | 说明 |
|------|---------|------|
| `src/js/ui.js` | 修改 | 添加性能监控功能 |
| `index.html` | 修改 | 更新 footer 版本号 |
| `src/index.html` | 修改 | 更新 footer 版本号 |
| `CHANGELOG.md` | 修改 | 新增 v1.7.5 条目 |

## 性能监控使用示例

### 查看性能日志
打开浏览器控制台（F12），执行以下命令：
```javascript
// 查看最近 10 条性能记录
ForestCalc._perfLog.slice(-10)

// 清空性能日志
ForestCalc._perfLog = []
```

### 典型输出
```
[Perf] renderBatch: 12.34 ms
[Perf] calcBatch: 8.56 ms
[Perf] renderBatch: 0.78 ms  (增量更新)
```

## 后续计划
- **v1.7.6**: 虚拟滚动实现（处理 1000+ 行场景）
- **v1.7.7**: Web Worker 计算分离（避免阻塞 UI 线程）
- **v1.7.8**: 性能监控面板 UI（可视化展示性能数据）

## 安装指引

### 全新安装
1. 下载 `release/ForestCalculator_v1.7.5.zip`
2. 解压到任意目录
3. 用浏览器打开 `index.html`

### 从 v1.7.4 升级
**只需覆盖文件**：
- `src/js/ui.js`
- `index.html`
- `src/index.html`

**不需要**：
- 不需要重新录入数据（localStorage 兼容）
- 不需要修改配置

## 验证清单
- [ ] footer 显示 `ForestCalculator v1.7.5`
- [ ] 打开控制台，执行 `ForestCalc._perfLog` 返回数组
- [ ] 添加/删除行，控制台输出 `[Perf] renderBatch: X.XX ms`
- [ ] 点击"计算合计"，控制台输出 `[Perf] calcBatch: X.XX ms`
- [ ] 单株计算功能正常
- [ ] 批量计算功能正常
- [ ] CSV 导入功能正常
- [ ] 收方表生成正常
- [ ] 跨省对比功能正常

## Git 提交历史
- `4c8f2a1` — v1.7.5: 性能监控集成
- `v1.7.5` — GitHub Release 已创建

## 文件完整性校验
```
ForestCalculator_v1.7.5.zip
├── index.html          (v1.7.5)
├── src/
│   ├── index.html      (v1.7.5)
│   ├── css/style.css
│   └── js/
│       ├── ui.js       (性能监控 + _logPerf)
│       ├── calculator.js
│       ├── species-db.js
│       ├── storage.js
│       └── app.js
├── data/
├── docs/
└── CHANGELOG.md       (v1.7.5 条目已添加)
```

## 回滚方案
如果 v1.7.5 出现问题，可回滚到 v1.7.4：
1. 下载 `ForestCalculator_v1.7.4.zip`
2. 覆盖 `src/js/ui.js`
3. 或执行 `git checkout v1.7.4`

## 已知问题
- 性能监控面板 UI 尚未实现（计划 v1.7.8）
- 虚拟滚动尚未实现（计划 v1.7.6）
- Web Worker 尚未实现（计划 v1.7.7）

## 技术支持
- GitHub Issues: https://github.com/CNJooko/ForestCalculator/issues
- 邮箱: [维护者邮箱]

---
*报告生成时间: 2026-06-09 17:45*
*作者: ForestCalculator 开发团队*
