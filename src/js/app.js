// ========== 应用入口 — 事件绑定与初始化 ==========
// 依赖 species-db.js, storage.js, calculator.js, ui.js

// 页面加载完成后初始化应用和绑定事件
document.addEventListener('DOMContentLoaded', function() {
  ForestCalc.initApp();
});