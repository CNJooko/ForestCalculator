# ForestCalculator v1.7.6 版本更新报告

## 版本概览
- **版本号**: v1.7.6
- **发布日期**: 2026-06-09
- **版本主题**: 虚拟滚动实现
- **前序版本**: v1.7.5

## 本次更新内容

### 核心功能：虚拟滚动
重构 `renderBatch()` 函数，实现虚拟滚动以处理 1000+ 行场景。

**实现方式**：
- 新增 `_renderVirtualBatch()`: 虚拟滚动核心函数
- 新增 `_renderIncrementalBatch()`: 原有增量更新逻辑（行数 ≤ 50 时使用）
- 新增 `_bindVirtualScroll()`: 绑定滚动事件 + 16ms 防抖（~60fps）
- 阈值判定: 行数 > 50 自动启用虚拟滚动

**技术原理**：
- 固定行高 52px（与 CSS 保持一致）
- 只渲染可视区域 + 缓冲区（上下各 5 行）
- 使用绝对定位 + top 值定位行
- 通过 `_batchRowPool` 缓存池复用已创建的行 DOM
- 通过 `dataset` 属性缓存行状态实现变更检测
- 窗口 resize 时自动重新计算

### 修改的文件
| 文件 | 修改类型 | 说明 |
|------|---------|------|
| `src/js/ui.js` | 重构 | 拆分 renderBatch() 为增量渲染 + 虚拟滚动 |
| `src/css/style.css` | 修改 | 添加 position: relative 为虚拟滚动定位基准 |
| `index.html` | 修改 | 更新 footer 版本号 |
| `src/index.html` | 修改 | 更新 footer 版本号 |
| `CHANGELOG.md` | 修改 | 新增 v1.7.6 条目 |

### 性能提升（理论估算 vs v1.7.4）
| 行数 | 渲染模式 | 首次渲染 | 增量更新 |
|------|---------|---------|---------|
| ≤50 | 增量更新 | ~相同 | ~相同 |
| 100 | 增量更新 | ~相同 | ~相同 |
| 500 | 虚拟滚动 | 提升 60-80% | 提升 70-80% |
| 1000 | 虚拟滚动 | 提升 80-90% | 提升 70-80% |
| 5000 | 虚拟滚动 | 提升 95%+ | 提升 70-80% |

### 完整版本演进
| 版本 | 主题 |
|------|------|
| v1.7.1 | CSV/JSON 导入导出 (WPS 兼容) |
| v1.7.3 | 架构审计与缺陷修复 |
| v1.7.4 | renderBatch() 增量更新 |
| v1.7.5 | 性能监控集成 |
| **v1.7.6** | **虚拟滚动实现** |

## 安装指引

### 全新安装
1. 下载 `release/ForestCalculator_v1.7.6.zip`
2. 解压到任意目录
3. 用浏览器打开 `index.html`

### 从 v1.7.5 升级
覆盖以下文件：
- `src/js/ui.js`
- `src/css/style.css`
- `index.html`
- `src/index.html`

**不需要**：
- 不需要重新录入数据
- 不需要修改配置

## 验证清单
- [ ] footer 显示 `ForestCalculator v1.7.6`
- [ ] ≤50 行时使用增量更新模式
- [ ] >50 行时启用虚拟滚动（footer 显示"虚拟滚动已启用"）
- [ ] 滚动流畅，无闪烁
- [ ] 行编辑功能正常
- [ ] 行删除功能正常
- [ ] 计算合计功能正常
- [ ] CSV 导入功能正常
- [ ] 收方表功能正常
- [ ] 跨省对比功能正常

## 后续计划
- **v1.7.7**: Web Worker 计算分离（避免阻塞 UI 线程）
- **v1.7.8**: 性能监控面板 UI（可视化展示性能数据）

---
*报告生成时间: 2026-06-09*
*作者: ForestCalculator 开发团队*